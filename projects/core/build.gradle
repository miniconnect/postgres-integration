plugins {
    id 'java-library'
}

description = 'PostgreSQL integration core building blocks'

tasks.withType(JavaCompile).configureEach {
    options.release = 8
}

dependencies {
    api "hu.webarticum.miniconnect.api:lang:${miniConnectApiVersion}"
}
